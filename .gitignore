.DS_Store
*.pyc
__pycache__/
site/
synced_docs
repos/
sites/
.venv/
# Python autocontenido (python-build-standalone) por plataforma -- se
# descarga e instala con runtime/requirements.lock.txt, no se versiona.
runtime/*/
# Instaladores compilados -- se generan con installer/windows/marc.nsi,
# no se versionan.
dist/
webapp/config.json
webapp/page_index.json
webapp/__pycache__/
# node_modules de frontend/ (herramienta de dev -- ver frontend/README.md):
# se reinstala con npm install, no se versiona. El .js ya compilado en
# vendor/ si se versiona, ese es el que la app realmente usa.
frontend/node_modules/
